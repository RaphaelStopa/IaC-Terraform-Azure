# Login on: https://bridgecrew.io/
# You must use an API Token to do the integration (pipeline)
# It is still possible to run locally
# Need to install Python
# install Checkov (pip install checkov)
# Run checkov in folder. checkov -f (or -d for directory) appservice.tf